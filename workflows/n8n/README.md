# n8n Workflows

Store exported, sanitized n8n workflow definitions here.

Before committing an export:
- remove credentials and secrets;
- remove personal identifiers;
- use synthetic sample data;
- document required credentials separately;
- record the workflow version and purpose.
